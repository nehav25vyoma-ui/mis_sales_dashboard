MIS SALES PROJECT — USER PROMPTS
================================

Compiled chronologically from the project conversation.


1. ORIGINAL PROJECT BRIEF
-------------------------

Create 4 buttons on the left side:

1. Dashboard
2. Upload Center — for all 4 channels
3. Report Center
4. Upload History

Each button should be independent and should not depend on the others.

First, start working with Upload Center for all 4 channels.

I am building an MIS Sales Dashboard using React (Frontend), FastAPI
(Backend), Pandas (Data Processing), and PostgreSQL (Database).

Each module should be independent and reusable because the same workflow will
later be used for SFH, Amazon, and Direct Sales.

For now, implement only the DSG Upload Module.

Overall workflow:

1. User uploads the DSG dataset (Excel/CSV).
2. Validate the uploaded file.
3. Perform the existing data-cleaning process. Do not change the current
   cleaning logic.
4. Open the Category Review page.
5. After Category Review is completed, open the Product Review page.
6. Update the uploaded dataset with all user changes.
7. Save the final reviewed dataset into PostgreSQL.
8. Create an Upload History record.

Each step should have its own page and should not depend on other modules.

Step 1 — DSG Dataset Upload

- Allow the user to upload one DSG dataset.
- Validate mandatory columns.
- Read the file using Pandas.
- Execute the existing cleaning process.
- Do not modify any existing business logic or calculations.
- After successful validation, navigate to Category Review.

Step 2 — Category Review

Standardize all categories into:

- Books
- Web Version
- Audio Device
- Pen Drive

Automatic category mapping:

- Book → Books
- Paperback → Books
- Flipbook → Web Version
- Web Version → Web Version
- Audio Device → Audio Device
- Pen Drive → Pen Drive

Automatically updated records should not appear on Category Review.

After automatic mapping, display only records whose categories do not match
the standard categories.

Display:

- Order Number
- Product Name
- Original Category
- Amount (Item Cost × Quantity)
- Suggested Category dropdown

Dropdown values:

- Books
- Web Version
- Audio Device
- Pen Drive

When Update is clicked:

- Update Category in the uploaded dataset.
- Do not change Amount.
- Remove the updated record from the review list.
- Continue until all unknown categories are reviewed.
- Enable Proceed to Product Review only when all records are resolved.

Combo handling:

- Do not automatically map Combo.
- Display Combo separately for manual splitting.
- User manually enters split amounts.
- Split total must exactly equal the original amount.
- Display a validation error when totals do not match.
- Remove the original Combo row.
- Create new rows for the split categories.
- Copy every other original column.
- Update only Category and Amount.

Step 3 — Product Review

- Open after Category Review.
- Standardize product names.
- Detect similar names using case-insensitive comparison and fuzzy matching.
- Example: APPLE, Apple, apple, Apple090, and 909Apple represent the same
  product.
- Display Standard Product Name and detected variations.
- Let the user enter the correct product name.
- Update every matching variation in the dataset immediately.
- Continue until all groups are reviewed.

Step 4 — Save Dataset

- Save the reviewed dataset into PostgreSQL.
- Preserve all original columns.
- Do not modify existing calculations.
- Store updated Category and Product Name values.

Step 5 — Upload History

Store:

- Upload ID
- File Name
- Channel = DSG
- Upload Date and Time
- Uploaded By
- Total Records Uploaded
- Upload Status

Create separate pages:

- DSG Upload
- Category Review
- Product Review
- Upload Success

Progress indicator:

Upload → Category Review → Product Review → Save Dataset → Completed

Validation:

- Validate mandatory columns.
- Show meaningful errors for invalid files.
- Combo split total must equal the original amount.
- Unknown categories cannot be skipped.
- Product Review must finish before saving.
- Saving is disabled until every review is complete.

Important:

- Do not change existing upload logic, cleaning logic, calculations, or report
  generation.
- Apply Category and Product Name changes to the uploaded dataset before
  saving.
- Keep the workflow modular for SFH, Amazon, and Direct Sales.
- Follow React, FastAPI, Pandas, and PostgreSQL best practices.


2. PROJECT CONTINUATION REQUEST
-------------------------------

Update this for my current project. We will update the project step by step.
Only the current step has been done; now move to the next step and implement
this project.


3. RUNNING THE PROJECT
----------------------

How do I run the project?

I received:

{"message":"MIS Sales Backend is Running"}

Give the full running steps because I closed all terminals. Currently the
backend is running.

I opened localhost:5173. What next?

After clicking OK, nothing changes. I remain on the same page and Category
Review is not performed.


4. CATEGORY REVIEW UPDATE
-------------------------

Update Category Review as follows:

- Standardize categories into Books, Web Version, Audio Device, and Pen Drive.
- Automatically map:
  - Book - Paperback → Books
  - Web Version → Web Version
  - Flipbook → Web Version
  - Audio Device → Audio Device
  - Pen Drive → Pen Drive
- Automatically update these categories in the dataset.
- Do not display automatically mapped rows on Category Review.
- Display only unknown categories and Combo categories.
- Show:
  - Order Number
  - Product Name
  - Original Category
  - Amount (Item Cost × Quantity)
  - Suggested Category dropdown
- Dropdown values:
  - Books
  - Web Version
  - Audio Device
  - Pen Drive
- Update Category immediately when Update is clicked.
- For Combo, manually split the amount across appropriate standard categories.
- Split total must equal the original amount.
- Replace the original Combo row with split rows.


5. AUTOMATIC REVIEW DETECTION AND LEARNING PATH
-----------------------------------------------

Before implementing Product Review:

- Standardize Learning Path to Web Version automatically.
- Do not include Learning Path records in Category Review.
- Automatically detect whether the uploaded dataset requires Category Review,
  Product Review, both, or neither.
- Display only the review pages required by the dataset.
- Implement Product Review as the next step.


6. PRODUCT REVIEW LAYOUT — FIRST UPDATE
---------------------------------------

In Product Review, when Detected Variations = 2, Standard Product Name should
also provide two options.

Place Detected Variations on the left and Standard Product Name on the right.


7. PRODUCT REVIEW LAYOUT — ROW MAPPING
--------------------------------------

On the right side, provide the same number of rows as Detected Variations.

- Each detected variation should occupy one row on the left.
- The same row should contain an editable Standard Product Name field on the
  right.
- Provide a typing option for every Standard Product Name.


8. PRODUCT REVIEW SAVE AND UPLOAD HISTORY
-----------------------------------------

When Update All Records is clicked:

- Respond dynamically.
- Update the original uploaded dataset.
- After all product reviews are completed, store the reviewed dataset in
  PostgreSQL.
- Display the saved dataset in Upload History.
- Standardize the DSG format in PostgreSQL.
- The DSG upload should not accept a different dataset format.
- Display a popup for an invalid dataset.
- If a dataset is deleted from Upload History, delete it from PostgreSQL too.
- Create a unique ID for every dataset to prevent duplication.


9. PRODUCT REVIEW FEEDBACK
--------------------------

After updating, there is no response. How does the user know whether it was
updated?

After all product reviews are updated, display a clear message such as:

All Product Review Done


10. PRODUCT REVIEW WORKFLOW FIX
-------------------------------

Fix Product Review. It is not working correctly.

- Make Update work the same way as Category Review.
- When Update is clicked, immediately update Product Name in the uploaded
  dataset.
- Remove the updated record/group from the review list.
- Update the pending review count at the top-right.
- Continue until all product reviews are complete.
- When pending count reaches zero, automatically enable or navigate to Save
  Dataset.
- Ensure reviewed product names are in the dataset before PostgreSQL storage.


11. POSTGRESQL VERIFICATION QUESTIONS
-------------------------------------

How can I see the saved dataset in PostgreSQL?

How can I verify SFH data in PostgreSQL?

Explain the purpose of the query used to view the saved SFH dataset.

Confirm that the system does not remove original columns. It should update only
the mentioned columns, add required columns, and store the result in the
database.

How can I view row_data?

Does row_data contain the modified Category and Course values?

How can I properly view all column data?


12. SFH UPLOAD WORKFLOW
-----------------------

Implement the SFH Upload workflow.

After SFH upload:

Category Review:

- Skip manual Category Review.
- Automatically set Category to Web Version for every record.
- Do not display Category Review for SFH.

Product Review:

- Open Product Review after automatic Category update.
- Use the Course column as Product Name.
- Display Course values for product standardization.
- Product Review should behave exactly like DSG:
  - Update button
  - Pending count
  - Dataset update
  - Completion flow

Save Dataset:

- After Product Review, save the updated dataset to PostgreSQL.
- Ensure all Product Name changes are reflected before saving.
- Do not change any other existing business logic or calculations.


13. DSG KPI CARD DASHBOARD
--------------------------

Update the DSG KPI Card Dashboard.

Filter behavior:

Mode 1 — All Channels

- Display channel-wise breakdown.
- Future channel names:
  - Online Sales (DSG)
  - Website Sales (SFH)
  - Amazon
  - Direct Sales
- Display only channels with data.
- KPI headline shows the combined total.

Mode 2 — Individual Channel

- Display category-wise breakdown instead of channel-wise breakdown.
- Do not repeat the selected channel name inside the breakdown.

DSG KPI calculations use the reviewed DSG dataset.

0 Rated Sales:

- Use Country Code (Billing).
- Initially requested logic: Country Code (Billing) ≠ INR.
- Sum Item Cost × Quantity.
- Subtitle: Non-INR Foreign Sales.

Exempted Sales:

- Category = Books.
- Sum Item Cost × Quantity.
- Subtitle: No tax applied.

Taxable Amount:

- Categories:
  - Web Version
  - Audio Device
  - Pen Drive
- Sum Item Cost × Quantity.
- Subtitle: Audio Device, Pen Drive, Web Version.

Sales as per P&L:

Sales as per P&L = 0 Rated Sales + Exempted Sales + Taxable Amount

Subtitle: 0 Rated + Exempted + Taxable.

Breakdown for All Channels:

- Online Sales (DSG)
- Website Sales (SFH)
- Amazon
- Direct Sales
- Total

Display only channels containing data.

Breakdown for Online Sales (DSG):

0 Rated Sales:

- Web Version
- Audio Device
- Pen Drive
- Total

Exempted Sales:

- Books
- Total

Taxable Amount:

- Web Version
- Audio Device
- Pen Drive
- Total

Sales as per P&L:

- 0 Rated Sales
- Exempted Sales
- Taxable Amount
- Total

Trend:

- All Channels → combined monthly trend.
- Online Sales (DSG) → DSG monthly trend.
- Keep existing sparkline design.
- No axes or gridlines.


14. CHANNEL-SPECIFIC AMOUNT LOGIC
---------------------------------

Update Amount calculations based on the selected channel.

DSG:

- Always use Item Cost × Quantity as Amount.
- All KPIs, category totals, product totals, and reports must use it.

SFH:

- Determine Amount using Currency.
- If Currency contains the literal ₹ symbol:
  - Treat it as INR.
  - Use Without Tax Total as Amount.
- If Currency contains any value other than ₹:
  - Treat it as Non-INR.
  - Use Earnings as Amount.
- Do not confuse the text INR with the ₹ symbol.
- Apply this consistently across KPIs, reports, tables, and calculations.
- Do not change other business logic.


15. FINAL DSG KPI CALCULATION
-----------------------------

Update DSG KPI calculations using the reviewed DSG dataset.

Always use Item Cost × Quantity.

0 Rated Sales:

- Use Country Code (Billing).
- Exclude records where Country Code (Billing) = IN.
- Include every other country code.
- Sum Item Cost × Quantity.

Exempted Sales:

- Category = Books.
- Sum Item Cost × Quantity.

Taxable Amount:

- Categories:
  - Web Version
  - Audio Device
  - Pen Drive
- Sum Item Cost × Quantity.

Sales as per P&L:

Sales as per P&L = 0 Rated Sales + Exempted Sales + Taxable Amount

Important:

- Use the reviewed DSG dataset.
- Use updated Category values.
- Use Item Cost × Quantity.
- All KPI totals and expanded breakdowns must use the same logic.
- Breakdown totals must exactly match headline values.
- Do not modify other business logic or report generation.


16. DSG CALCULATION CORRECTION
------------------------------

The DSG calculation is wrong.

Displayed value: 1,09,728.98

Actual value: 103067

Review and correct the calculation.


17. NUMBER FORMATTING
---------------------

Keep all numbers as whole numbers.


18. PROMPT EXPORT
-----------------

Whatever prompts I have provided, share them in TXT format.


19. ONGOING PROMPT STORAGE RULE
-------------------------------

Every time I provide a new prompt, store it in TXT format.

Append every future user prompt chronologically to:

docs/project-prompts.txt


20. DEDICATED PROMPT FOLDER
---------------------------

Create a folder for the prompts and keep all prompts inside it in TXT format.

New prompt storage location:

docs/prompts/project-prompts.txt


21. PROMPT FOLDER NAME
----------------------

Can you tell me the folder name?


22. KPI SPARKLINE ENHANCEMENT
-----------------------------

Enhance the sparkline inside every KPI card:

1. Add a delta badge beside the headline showing latest-period versus
   previous-period percentage change.
   - Positive: green with up-arrow styling.
   - Negative: orange/red.
   - No change or insufficient prior data: neutral gray with "—".
   - Format percentages to one decimal place, such as +7.2% or -3.1%.

2. Match sparkline color to direction:
   - Green for upward trend.
   - Orange/red for downward trend.
   - Gray for flat or neutral.

3. Display one solid marker only on the final data point. Its color must match
   the line.

4. Keep the chart minimal:
   - No axes.
   - No gridlines.
   - No legend.

5. For all-zero or insufficient trends, show a flat dashed line or an em dash
   rather than a misleading moving line.

6. Synchronize trend and delta with channel filters:
   - All Channels uses the combined trend.
   - A selected channel uses only its own trend.

The time series should contain at least two periods for delta calculation and
ideally six or more for a meaningful trend shape.


23. INDEPENDENT TIME-GRAIN FILTER
---------------------------------

Add a second independent KPI Dashboard filter for time grain alongside the
channel filter.

Time-grain options:

- Monthly
- Quarterly
- Yearly

Default to Monthly, or the grain with the most complete available data.

Both filters apply together. For example, Website Sales plus Quarterly shows
Website Sales data aggregated by quarter.

Aggregation:

- Monthly: one point per month.
- Quarterly: sum the three months in each calendar quarter.
- Yearly: sum all months in each year.
- Filter by channel first, then aggregate by time grain.

Sparkline and delta:

- Plot all aggregated points available for the selected grain.
- Delta always compares the latest aggregated point with the immediately
  previous aggregated point at that grain.
- If fewer than two aggregated points exist, hide both sparkline and delta
  badge.
- Display "Not enough data at this grain yet" instead.

Card dimensions must remain fixed regardless of filter selection or data
volume.

Show Channel and Time Grain as two clearly labelled, independent control
groups.

Keep monthly data as the single source of truth and derive quarterly/yearly
aggregations rather than storing separate copies.


24. DASHBOARD VALUES STILL NOT UPDATED
--------------------------------------

Still not updated. The dashboard screenshot shows zero KPI cards while the
Sales Trend contains sales through July 2026.


25. SALES TREND SCREENSHOT MATCH
--------------------------------

Update the Sales Trend like in the provided screenshot.


26. SALES TREND PLAN TOOLTIP
----------------------------

In the Sales Trend tooltip, add the plan amount along with the sales amount.


27. SALES TREND COMPARISON AND CHANNEL FILTER
---------------------------------------------

Show the sales-versus-plan comparison inside the Sales Trend tooltip and add
an independent channel filter to the Sales Trend visual.


28. DYNAMIC CHANNEL PLANS IN SALES TREND
----------------------------------------

When a particular channel is filtered, dynamically update the plan using
these monthly amounts:

- DSG: 1,00,000
- SFH: 1,00,000
- Amazon: 1,00,000
- Direct Sales: 2,00,000


29. ACHIEVEMENT COLOR THEORY
----------------------------

Use color theory correctly for Sales Trend achievement comparisons. Red gives
a disproportionately negative impression when sales have achieved 94,519, or
about 95% of plan.


30. WEBPAGE LOADING PERFORMANCE
-------------------------------

The webpage takes too long to load. Identify the cause and fix the loading
performance.


31. DASHBOARD VISUAL RELOAD PERFORMANCE
---------------------------------------

Dashboard visuals still take too long to load when opening the Dashboard
button. Improve repeated dashboard loading performance.


32. DASHBOARD VISUALS STILL LOAD SLOWLY
---------------------------------------

Dashboard visuals still take a long time to load. Fix the issue beyond the
browser-session cache.


33. HOW TO FIX DASHBOARD LOADING TIME
-------------------------------------

How should the remaining dashboard visual loading time be fixed?


34. REDUCE VISUAL UPDATE LOADING TIME
-------------------------------------

How can the longer loading time when updating dashboard visuals be reduced?


35. IMPLEMENT LOADING-TIME REDUCTION
-------------------------------------

Reduce the dashboard loading-time issues.


36. FIVE-SECOND DASHBOARD TARGET
--------------------------------

Dashboard visuals should load in five seconds or less.


37. REUSABLE SALES VS PLAN TOOLTIP
----------------------------------

Build a reusable dark-themed React/TSX tooltip for a Sales vs Plan chart point.
It must accept period, planValue, salesValue, and an optional locale; calculate
gap, achievement, and status internally; use neutral Plan and Sales values;
show a status badge, Plan and Sales rows, capped progress bar with a white edge
tick and axis labels, and a plain-language above/below-plan verdict. Use Indian
digit grouping by default and the specified dark, amber, blue, softened-red,
and green color system.


38. KPI CARD ORDER AND HOVER
----------------------------

Update the MIS Sales Report KPI cards into this order: Total Sales, Books
Sales, Taxable Sales, Foreign Sales, and Total Orders. Highlight only the KPI
card being hovered or keyboard-focused, preserve responsive behavior and the
existing dashboard theme, and do not change any sales calculation logic.


39. SHORT CHANNEL DISPLAY NAMES
-------------------------------

Replace the displayed names “Online Sales (DSG)” with “DSG” and “Website
Sales (SFH)” with “SFH” throughout the entire dashboard, including KPI cards,
filters, charts, tables, tooltips, headers, navigation, reports, summaries, and
dynamically generated text. Do not change data, calculations, or business logic.


40. CATEGORY PERFORMANCE TABLE PRESENTATION
-------------------------------------------

Update only the visual styling and row-order controls for Category Wise
Performance: severity-scaled Var.% heat-map cells using theme roles, default
worst-variance-first sorting with Category A-Z option, Total pinned last and
visually separated, period-column visibility controls, and a divider between
Current and Comparison groups. Preserve every existing calculation and value.


41. VISUAL-ONLY CATEGORY TABLE RESTYLE
--------------------------------------

Apply the attached presentation specification only to the Category Wise
Performance table. Preserve its columns, data, calculations, and row order;
do not change other dashboard visuals or business logic.


42. CATEGORY TABLE SORT, CHANNEL FILTER, AND HOVER
-------------------------------------------------

Add ascending/descending sorting to every applicable Category Wise Performance
column; add an independent DSG, SFH, Amazon, and Direct Sales channel filter;
balance responsive column spacing; emphasize Variance % Against Plan; and
highlight only the individual hovered cell. Preserve calculations and business
logic.


43. CATEGORY TABLE SORT FILTER AND ALIGNMENT
--------------------------------------------

Match the screenshot by adding a visible Sort filter to Category Wise
Performance, removing OTT from its channel filter, and distributing Category
and metric columns evenly without affecting any other visual.


38. KPI CARD PLACEMENT
----------------------

Arrange the KPI cards in this order:

1. Total Sales
2. Books Sales
3. Taxable Sales
4. Foreign Sales
5. Total Orders


44. DASHBOARD PLACEMENT AND ALIGNMENT
-------------------------------------

All should be properly placed and aligned.


45. KPI COMPARISON BADGE ALIGNMENT
----------------------------------

The green comparison badges are not aligned properly. Align them consistently
across the KPI cards.


46. PLAN UPDATION
-----------------

Add a new “Plan Updation” option in the left-side menu.

Yearly Plan Management:

- Plans should be maintained separately for each year.
- The existing 2026 plan must remain unchanged when a new year's plan is added
  or updated.
- Support future years such as 2027 and 2028.

Year Selection:

- Show a Year Selection option first.
- After selecting the year, allow monthly plan entry and updates.

Provide separate channel-wise monthly plans for DSG, SFH, Amazon, Direct Sales,
OTT, and Language Lab.

Automatically calculate quarterly and yearly plans from the monthly plan. Do
not manually enter quarterly or yearly plans. Quarterly is monthly multiplied
by 3 and yearly is monthly multiplied by 12.

Dashboard Integration:

- Use the plan for the selected year.
- Month, quarter, and year filters must display the corresponding calculated
  plan.
- Respect the selected channel and time period.

Delete Option:

- Enable Delete only when a saved Year + Channel plan exists.
- Ask for confirmation before deletion.
- Delete only the selected Year + Channel record.
- Do not affect 2026 or any other year/channel.
- Allow a deleted plan to be added again.

Preserve Existing Plans:

- Do not overwrite, modify, or delete the existing 2026 plan.
- Store new plans separately by Year + Channel.

Database:

- Use a separate plan table containing Year, Channel, and Monthly Plan.
- Dynamically calculate monthly, selected-period, quarterly, and yearly plans.
- Enforce one record per Year + Channel and update the existing record instead
  of inserting a duplicate.

UI:

- Keep the dashboard design and functionality unchanged.
- Add only the Plan Updation menu and plan-management page.
- Provide clear Save/Update and Delete actions.
- Show success feedback after saving or updating.
- Refresh plan values after deletion without affecting other years.


47. DASHBOARD VALUES AFTER PLAN DELETION
----------------------------------------

After updating and then deleting a plan, the dashboard visual values are not
displaying. Restore the correct visual values after plan deletion.


48. CATEGORY AND CHANNEL PLAN UPDATION
--------------------------------------

Plan Updation should support both Category Plan Updation and Channel Plan
Updation.


49. CHANNEL PERFORMANCE PLAN NOT UPDATED
----------------------------------------

The 2027 plan has been updated, but the Channel Wise Performance visual is not
updating properly. Identify and explain the cause.


50. PLAN UPDATION STATUS
------------------------

Confirm whether Plan Updation was changed to use the Channel Wise Performance
names.


51. USE CHANNEL PERFORMANCE NAMES FOR PLANS
-------------------------------------------

Update Channel Plan Updation to use these exact Channel Wise Performance
names:

- Digital Online
- Stall Sales
- Direct Sales
- Bulk Sales
- Language Lab
- OTT


52. PRESERVE UNSAVED PLAN VALUES
--------------------------------

When one Channel or Category plan is saved, some other entered plan values
reset to zero. Fix both plan types so saving one record does not reset other
draft values. After saving, change the button label from “Update” to “Updated.”


53. TOTAL SALES KPI COMPARISON EXPLANATION
------------------------------------------

Explain how the comparison is updated in the Total Sales KPI card.


54. VERIFY AND UPDATE TOTAL SALES COMPARISON
--------------------------------------------

Check whether the Total Sales KPI comparison is updating correctly and fix it
where necessary.


55. FIVE-SECOND CROSS-DEVICE LOADING TARGET
-------------------------------------------

Overall loading time should be less than five seconds on the same device and
on different devices.


56. VISUAL-SPECIFIC FILTERS
---------------------------

Dashboard visual filters should affect only their own visual. Update this
without changing any other functionality, calculation, or business logic.


57. FILTER HIERARCHY AND NAVIGATION
-----------------------------------

Update dashboard navigation and filtering without changing calculations or
business logic:

- The global filter must apply to every dashboard button/page and visual.
- Each page should initially inherit the global filter.
- A page-specific override must affect only that page, not other pages.
- Returning to the global/overview page must reset the page override so it
  inherits the global filter again.
- A visual-specific filter must affect only that visual.


58. RESET VISUAL FILTERS ON TAB RETURN
--------------------------------------

When navigating from the first dashboard button to another tab and then back,
reset its visual-specific filters to the current All Channels/global filter.
Do not change any other functionality, calculation, or business logic.


59. FINANCIAL BREAKDOWN HORIZONTAL GRAPH
----------------------------------------

Update the horizontal line graph accurately only in Financial Breakdown. Do
not change any other functionality, calculation, visual, or business logic.


60. FINANCIAL BREAKDOWN TABLE BORDERS
-------------------------------------

Update the row and column borders in Financial Breakdown.


61. CHANNEL-WISE PERFORMANCE DIRECT SALES SPLIT
------------------------------------------------

Update only Channel-wise Performance so Direct Sales is split, after Digital
Online, into In Office, Stall, Bulk, Call, and Retail. Match the Invoice and
Sales Inventory datasets by mapped Invoice Number without duplicating sales.
Classify each invoice once using this priority: Stall, invoice-wise summed Qty
greater than 10 as Bulk, phone/ph no/call as Call, Vedanta as Retail, and In
Office as the default. Keep Digital Online and all other channel logic
unchanged. Use monthly plans of ₹50,000 for In Office, Stall, and Bulk, and
₹25,000 for Call and Retail with the existing year and period calculations.


62. REPORT CENTER DIRECT SALES OVERVIEW SPLIT
---------------------------------------------

Update the Report Center Direct Sales Overview to use the same Direct Sales
sub-channel classification as Channel-wise Performance without changing any
other functionality or business logic.


63. THREE-WAY CHANNEL PERFORMANCE WITH DETAILED REPORT
------------------------------------------------------

In Channel-wise Performance, show Direct Sales only as In Office, Stall, and
Bulk. Keep the existing Stall and Bulk calculations and include Call, Retail,
and every other unmatched Direct Sales transaction in In Office. Use monthly
plans of ₹1,00,000 for In Office and ₹50,000 each for Stall and Bulk. Keep the
Report Center Direct Sales Overview detailed as In Office, Stall, Bulk, Call,
and Retail without changing other functionality or logic.


64. CLARIFY DIRECT SALES REPORT QUANTITIES
------------------------------------------

Make Invoice Qty (Classification) less confusing because it is currently
shown for other Direct Sales types as well. Show the invoice-level quantity
only where it is relevant to Bulk classification, while retaining Product
Quantity for every report type.


65. REVERT REPORT QUANTITY PRESENTATION
---------------------------------------

Remove the most recent Direct Sales report quantity presentation update and
restore the previous Invoice Qty (Classification) behavior.


66. REVERT CHANNEL PERFORMANCE CONSOLIDATION
--------------------------------------------

Revert the most recent Channel-wise Performance update and restore the
previous detailed Direct Sales split and plans.


67. BULK BY MAPPED INVOICE QUANTITY
-----------------------------------

For Bulk, group Sales Inventory records by mapped Invoice Number, sum their
quantities, and classify the invoice as Bulk only when its summed quantity is
greater than 10.


68. BULK BY INDIVIDUAL INVENTORY QUANTITY
-----------------------------------------

Keep Bulk invoice-number based, but classify the mapped invoice as Bulk only
when an individual Sales Inventory Qty is greater than 10. Do not classify it
as Bulk merely because several smaller quantities sum to more than 10. Do not
change any other functionality or logic.


69. RETAIL EXEMPTION FROM BULK
------------------------------

For Retail only, do not apply the Bulk quantity calculation. A Vedanta
transaction should remain Retail even when its individual inventory quantity
is greater than 10. Do not change other functionality or logic.


70. DIRECT SALES OVERVIEW GRAND TOTAL
-------------------------------------

Add a Grand Total row at the end of the Direct Sales Overview report.


71. DIRECT SALES OVERVIEW SCREEN THEME
--------------------------------------

Update the Report Center screen theme for Direct Sales Overview without
changing its filters, calculations, downloads, or other application screens.


72. DIRECT SALES OVERVIEW EXCEL THEME
-------------------------------------

Apply a theme to the downloaded Direct Sales Overview report as well, with a
styled Excel layout while preserving its data and calculations.


73. SHARED THEME FOR ALL REPORTS
--------------------------------

Apply the same Excel theme to all Report Center downloads without changing
their data, formulas, sheets, filters, or calculations.


74. AMAZON CUSTOMER PERFORMANCE POPUP
-------------------------------------

When Customer Performance is clicked while Amazon is selected, show “No
customer performance details available for Amazon.” and do not open the page
or table. Keep Customer Performance unchanged for every other channel.


75. CUSTOMER PERFORMANCE BLANK TAB FIX
--------------------------------------

Check and fix the Customer Performance tab when it displays no content, while
preserving its existing functionality and the Amazon-only popup behavior.


76. AMAZON POPUP ON CUSTOMER CHANNEL CHANGE
-------------------------------------------

Fix the missing Amazon popup in Customer Performance and ensure it appears
both when opening Customer Performance with Amazon selected and when choosing
Amazon from the Customer Performance channel selector.


77. FILTER-AWARE DIRECT SALES GRAND TOTAL
-----------------------------------------

In the Direct Sales Overview Excel report, make the Grand Total dynamically
recalculate and display the total of only the rows visible after filtering.
